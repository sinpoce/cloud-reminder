# Base URL of your deployed Cloud Reminder worker (no trailing slash).
# Local dev can leave this empty — Vite proxies /api to localhost:8787.
# Production example: https://cloud-reminder-api.your-subdomain.workers.dev
VITE_API_BASE=
